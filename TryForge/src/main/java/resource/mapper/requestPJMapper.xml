<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="tryForge.admin.dao.PJRequestDao">
	
	<resultMap type="" id="team">
		
	</resultMap>
	<!-- 프로젝트 출력 -->
	<select id="">
		SELECT * FROM PROJECT p 
		LEFT JOIN TEAM t ON t.project_key = p.project_key
		LEFT JOIN TEAM_MEMBER tm ON tm.TEAM_KEY = t.TEAM_KEY
		LEFT JOIN MEMBER m ON m.MEMBER_KEY = tm.MEMBER_KEY
	</select>
</mapper>